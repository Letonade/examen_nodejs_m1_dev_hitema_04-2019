module.exports.decodeHexFileContent = (filePath) =>  {
    return new Promise((resolve, reject) => {
        // To be implemented!
        if (filePath === null) resolve(filePath);else reject(null);

    });
}